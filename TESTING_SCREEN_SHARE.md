# 화면 공유 기능 테스트 가이드

## 혼자서 테스트하기

### 방법: 일반 창 + 시크릿 창

#### 1단계: 창 준비
- **일반 창**: `http://localhost:5173/metaverse?role=instructor`
- **시크릿 창** (Ctrl+Shift+N): `http://localhost:5173/metaverse?role=student`

#### 2단계: 양쪽 모두 학교 맵 입장
- 포탈 근처로 이동 (F키) → 학교 입장

#### 3단계: 강사 - 화면 공유 시작
1. WASD로 교탁까지 이동
2. F키 - 교탁에 서기
3. 우측 상단 "화면 공유 시작" 버튼 클릭
4. 브라우저 팝업에서 공유할 화면 선택

#### 4단계: 학생 - 화면 보기
1. 우측 상단 "📺 강사님의 화면 보기" 버튼 확인
2. 버튼 클릭
3. 비디오 오버레이에 공유 화면이 표시되는지 확인

---

## 체크리스트

### ✅ 강사 창 (일반 창)

**화면:**
- [ ] 교탁에 서면 "화면 공유 시작" 버튼 표시
- [ ] 공유 시작 후 버튼이 "화면 공유 종료"로 변경 (빨간색)
- [ ] 좌측 상단에 "🧪 TEST: INSTRUCTOR" 표시

**콘솔 (F12):**
```
📺 Screen sharing started
📺 Offer sent to student {socketId}
📺 Answer received from {socketId}
📺 Connection state with {socketId}: connected
```

### ✅ 학생 창 (시크릿 창)

**화면:**
- [ ] 강사가 공유 시작하면 "화면 보기" 버튼 표시 (초록색, 빨간 점 깜빡임)
- [ ] 버튼 클릭 시 우측에 비디오 오버레이 표시
- [ ] 비디오에 공유 화면이 재생됨
- [ ] 좌측 상단에 "🧪 TEST: STUDENT" 표시

**콘솔 (F12):**
```
📺 강사 started screen sharing
📺 Received offer from teacher {socketId}
📺 Answer sent to teacher {socketId}
📺 Connection state with teacher: connected
📺 Received track from teacher
```

---

## 테스트 시나리오

### 시나리오 1: 기본 화면 공유
1. ✅ 강사가 화면 공유 시작
2. ✅ 학생이 "화면 보기" 클릭
3. ✅ 비디오 재생 확인
4. ✅ 최소화 버튼 클릭 → 우측 하단으로 작아짐
5. ✅ 다시 클릭 → 원래 크기로 복원
6. ✅ 전체화면 버튼 → 전체화면 모드
7. ✅ ESC → 전체화면 해제

### 시나리오 2: 공유 중지 (강사 버튼)
1. ✅ 강사가 "화면 공유 종료" 버튼 클릭
2. ✅ 학생 쪽 비디오 오버레이 자동 사라짐
3. ✅ 학생 쪽 "화면 보기" 버튼도 사라짐

### 시나리오 3: 공유 중지 (브라우저 UI)
1. ✅ 강사 창 상단의 "공유 중지" 버튼 클릭
2. ✅ 학생 쪽 자동 종료

### 시나리오 4: 교탁에서 내려오기
1. ✅ 화면 공유 중 F키로 일어서기
2. ✅ 공유 자동 중지
3. ✅ 학생 쪽 자동 종료

### 시나리오 5: 여러 학생
1. ✅ 시크릿 창 2개 이상 열기 (모두 student)
2. ✅ 모든 학생에게 "화면 보기" 버튼 표시
3. ✅ 각각 독립적으로 보기/닫기 가능

---

## 문제 해결

### 문제: "화면 공유 시작" 버튼이 안 보임

**원인:**
- 교탁에 서지 않았음
- URL에 `?role=instructor` 없음

**해결:**
1. URL 확인: `http://localhost:5173/metaverse?role=instructor`
2. 학교 맵 입장 확인
3. 교탁 위치로 이동: X: -52.17, Z: -28.20 근처
4. F키로 교탁에 서기
5. 좌측 상단에 "🧪 TEST: INSTRUCTOR" 표시 확인

### 문제: "화면 보기" 버튼이 안 보임

**원인:**
- 강사가 아직 공유를 시작하지 않음
- 같은 방에 있지 않음
- Socket 연결 문제

**해결:**
1. 강사 창에서 화면 공유가 시작되었는지 확인
2. 양쪽 모두 학교 맵에 입장했는지 확인
3. 콘솔에서 `📚 Room users:` 로그 확인 (양쪽 모두 표시되어야 함)
4. 페이지 새로고침 (Ctrl+Shift+R)

### 문제: 버튼 클릭해도 비디오가 안 보임

**원인:**
- WebRTC 연결 실패
- 강사가 화면 선택을 안 했거나 취소함

**해결:**
1. **양쪽 콘솔** 확인:
   - 강사: `📺 Answer received from {socketId}` 있어야 함
   - 학생: `📺 Received track from teacher` 있어야 함
2. 연결 상태 확인:
   - `Connection state: connected` 로그 확인
3. 강사 창에서 화면 공유를 다시 시작
4. 방화벽/보안 프로그램 확인

### 문제: "Screen sharing stopped" 로그가 계속 나옴

**원인:**
- 브라우저 캐시 문제
- 역할 파라미터 없이 접속

**해결:**
1. 브라우저 강력 새로고침: `Ctrl + Shift + R`
2. URL에 역할 파라미터 추가: `?role=instructor` 또는 `?role=student`
3. 캐시 완전 삭제 후 다시 시작

### 문제: 비디오가 검은 화면만 나옴

**원인:**
- 강사가 잘못된 화면/탭을 선택함
- 공유 권한 문제

**해결:**
1. 강사가 화면 공유를 중지하고 다시 시작
2. 브라우저 화면 공유 팝업에서 올바른 탭/창 선택
3. Chrome 설정 → 개인정보 및 보안 → 사이트 설정 → 화면 공유 권한 확인

### 문제: 연결은 되는데 화질이 너무 나쁨

**원인:**
- 네트워크 속도 문제
- 로컬호스트라서 문제는 아닐 것임

**해결:**
1. `useScreenShare.js`에서 해상도 낮추기:
```javascript
video: {
  width: { ideal: 1280 },  // 1920에서 낮춤
  height: { ideal: 720 },  // 1080에서 낮춤
  frameRate: { ideal: 24 },  // 30에서 낮춤
}
```

### 문제: 소켓 연결 에러

**원인:**
- 백엔드 서버가 실행되지 않음

**해결:**
1. 터미널에서 확인:
```bash
npm run dev:both
```
2. 백엔드 포트 확인: http://localhost:3000/health
3. Socket.IO 연결 확인 (콘솔에서)

---

## 디버깅 팁

### Chrome DevTools Network 탭
1. F12 → Network 탭
2. WS (WebSocket) 필터 클릭
3. Socket.IO 연결 상태 확인

### WebRTC Internals
Chrome에서:
```
chrome://webrtc-internals/
```
- PeerConnection 상태 확인
- ICE candidate 교환 확인
- 비디오 스트림 통계 확인

### 양쪽 콘솔 동시 확인
- 모니터 2개 또는 창 배치로 양쪽 콘솔을 동시에 확인
- 이벤트 흐름 파악

---

## 성공 기준

모든 시나리오가 통과하면 ✅ 구현 완료!

1. ✅ 강사가 교탁에 서면 버튼 표시
2. ✅ 화면 공유 시작 시 학생에게 알림
3. ✅ 학생이 수동으로 "화면 보기" 클릭
4. ✅ 실시간 비디오 스트림 재생
5. ✅ 최소화/전체화면 UI 작동
6. ✅ 강사가 중지하면 학생도 자동 종료
7. ✅ 교탁에서 내려오면 자동 종료
8. ✅ 여러 학생 동시 수신 가능
