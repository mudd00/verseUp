## Progress Summary (2025-11-20)
- Git root moved to top-level; frontend `verseUp/`, backend `verseUp-server/`.
- MapModel: removed try-catch around `useGLTF` (Suspense), added ErrorBoundary+Suspense fallback; logging/material tweaks; scale-down if huge; floor alignment; render scene separately from collisions.
- Collision: large invisible cuboid floor collider (500x1x500, y=-0.5); scene uses visual primitive only.
- Player/physics: start y=10, ball collider, friction=1, restitution=0; `CharacterModel` gets `isMoving` from WASD.
- Camera: pointer lock; click canvas to lock, ESC to unlock; `movementX/Y` drive azimuth/polar; cursor hidden when locked.
- CharacterModel: loads Mixamo `Walking.fbx` via FBXLoader; scale 0.01; shadows on; animations controlled by `isMoving` (fade in/out, stop when idle).
- Known warnings safe to ignore: Rapier deprecated init params; FBX shininess map; >4 skin weights (clamped).

## Next AI Prompt
We have a monorepo root; frontend is in `verseUp`, backend in `verseUp-server`. Git root is top-level.

Frontend state (2025-11-20):
- MapModel.tsx: no try-catch around useGLTF; wrapped with ErrorBoundary + Suspense fallback; logging/material fixes; scales down huge models; floor aligned. Uses large invisible cuboid floor collider (500x1x500 at y=-0.5); scene renders separately.
- Player: start y=10; ball collider; friction=1; restitution=0; CharacterModel receives `isMoving` from WASD.
- Camera: ThirdPersonCamera uses pointer lock (click to lock, ESC to unlock); `movementX/Y` drive azimuth/polar; cursor hidden when locked.
- CharacterModel.tsx: loads Mixamo `Walking.fbx` via FBXLoader; scale 0.01; casts/receives shadows; animations controlled via `isMoving` (fadeIn/out then stop when idle).
- Warnings about Rapier params, FBX shininess, and >4 skin weights are known/harmless.

Requested next task:
- Use idle FBX at `public/models/Standing Idle.fbx` as idle animation.
- Switch to `Walking.fbx` when moving (WASD).
- Keep current structure; no other changes.
