# 프로젝트 완료 보고서

| 프로젝트 주제 | 메타버스 학습 플랫폼 (VerseUp!) |
|------------|---------------------------|
| 단계 | 프로젝트 완료 |
| 작성자 | 조민혁 |
| 작성일 | 2025.12.XX |

---

# 3차 프로젝트 완료 보고서

## 메타버스 학습 플랫폼 (VerseUp!)

**2025.12.XX**

**구성원**
- 안지민
- 이재원
- 조민혁

---

## 문서 개정 이력

| 개정번호 | 개정일자 | 시행일자 | 개정내용 | 담당자 |
|:-------:|:-------:|:-------:|:-------:|:------:|
| 1.0 | 2025.12.XX | 2025.12.XX | 최초 작성 | 조민혁 |

**교육기관**: 한국정보교육원
**팀명**: (팀명)
**팀장**: 조민혁
**팀원**: 안지민, 이재원

---

## 목차

1. 프로젝트 개요
2. 프로젝트 추진 체계
3. 시스템 설계 및 아키텍처
4. 주요 구현 기능
5. 핵심 기술 구현
6. 배포
7. 개별 후기

---

# 1. 프로젝트 개요

## 프로젝트 명

**VerseUp! - 메타버스 학습 플랫폼**

## 프로젝트 기간

2025.11.13 ~ 2025.12.XX (총 XX일)

## 프로젝트 목표

가) 몰입감 있는 3D 메타버스 기반 온라인 학습 환경 구축

나) 실시간 상호작용이 가능한 화상 강의 시스템 구현

다) 학생-강사-학부모 간 유기적인 교육 생태계 구성

라) 직관적인 학습 관리 및 진도 추적 시스템 제공

## 프로젝트 시나리오

### 가) 가정

코로나19 이후 온라인 교육이 일상화되면서, 단순 영상 강의를 넘어 실제 교실에서 수업받는 듯한 몰입감 있는 학습 환경에 대한 수요가 증가하고 있습니다. 또한 학부모들의 자녀 학습 과정 참관에 대한 니즈가 존재합니다.

### 나) 대상

- 온라인 수업을 듣는 학생들
- 온라인 강의를 진행하는 강사들
- 자녀의 학습 과정을 확인하고 싶은 학부모들
- 플랫폼을 관리하는 운영자(관리자)

### 다) 세부내용

"새로운 학습 경험을 제공하는 웹 메타버스 교육 플랫폼" 구현

- React 19 + Three.js 기반 3D 가상 교실 환경 구성
- Supabase 클라우드를 통한 시스템의 안정성 및 확장성 확보
- RLS 정책을 통한 사용자별 데이터 접근 제한 및 권한 관리
- JWT 토큰 기반 인증을 통한 비인가 사용자의 접근 통제
- Supabase Auth를 활용한 중앙 집중적인 사용자 인증 설정
- Socket.IO를 통한 실시간 멀티플레이어 상호작용
- WebRTC를 통한 화면 공유 및 실시간 커뮤니케이션
- 토스페이먼츠 연동을 통한 검증된 결제 시스템 구축
- 학부모 참관 모드를 통한 투명한 교육 환경 제공

## 프로젝트 수행요건

### 가) 기술적 요구사항

- 최신 웹 기술 스택을 활용한다. (React 19, Three.js, Supabase)
- 실시간 3D 렌더링 및 물리 엔진을 구현한다. (React Three Fiber, Rapier)
- 실시간 멀티플레이어 동기화 시스템을 구현한다. (Socket.IO)
- 안전한 결제 시스템을 연동한다. (토스페이먼츠)
- 확장 가능한 모노레포 아키텍처를 구현한다.

### 나) 비즈니스 요구사항

- 4가지 사용자 역할별 차별화된 기능을 제공한다. (학생/강사/관리자/학부모)
- 직관적이고 사용하기 쉬운 UI/UX를 제공한다.
- 반응형 웹 디자인을 구현한다.
- 데이터 보안 및 개인정보 보호를 제공한다.

## 개발 환경 및 도구

### 가) 개발 도구

| 구분 | 도구 |
|------|------|
| IDE | Visual Studio Code |
| 버전관리 | Git, GitHub |
| 프로젝트 관리 | GitHub Issues |
| 디자인 | Figma |
| 3D 모델링 | Blender |

### 나) 개발 환경

| 구분 | 환경 |
|------|------|
| Node.js | v18.x |
| Package Manager | npm |
| Database | Supabase (PostgreSQL) |
| 배포 | (배포 환경) |

## 최종 결과물

**프로덕션 URL**: (배포 URL)

---

# 2. 프로젝트 추진 체계

## 프로젝트 참여 인력 총괄표

| 성명 | 소속 | 역할 | 담당 업무 |
|:----:|:----:|:----:|:--------:|
| 안지민 | 한국정보교육원 | Project Leader | 역할 배분, 일정 관리, 아키텍처 개발, 통합 테스트 |
| 이재원 | 한국정보교육원 | Engineer | 메타버스 / 강사 시스템 개발, 기능 테스트 |
| 조민혁 | 한국정보교육원 | Engineer | 학생 / 관리자 / 학부모 시스템 개발, 기능 테스트 |

## 참여 인력 업무 분장

| 업무명 | 업무내용 |
|:------:|:--------|
| 보고서 작성 | - 프로젝트 기획서 작성<br>- 프로젝트 결과 보고서 작성<br>- 프로젝트 진행 상황에 대한 일정 조정<br>- 기타 보고서 작성 및 발표 진행 |
| 시스템 아키텍처 구성 | - 전체 시스템 아키텍처 설계<br>- 모노레포 구조 설계 및 구현<br>- 프론트엔드와 백엔드 통합 구성<br>- 클라우드 인프라 구성 |
| 프론트엔드 개발 | - React 19 기반 사용자 인터페이스 구현<br>- Three.js 기반 3D 메타버스 환경 구현<br>- 학생/강사/관리자/학부모별 화면 구성<br>- 반응형 웹 디자인 구현<br>- 상태 관리 및 라우팅 구성<br>- UI/UX 최적화 구현 |
| 백엔드 개발 | - Supabase 데이터베이스 스키마 설계<br>- Express.js 기반 RESTful API 서버 구현<br>- Socket.IO 실시간 통신 시스템 구현<br>- 인증 및 권한 관리 시스템 구성<br>- RLS 보안 정책 구현<br>- 결제 시스템 연동<br>- 파일 업로드/다운로드 구현 |
| 3D 메타버스 개발 | - React Three Fiber 기반 3D 씬 구성<br>- Rapier 물리 엔진 적용<br>- 캐릭터 이동 및 애니메이션 구현<br>- 멀티플레이어 동기화 구현<br>- 인터랙티브 오브젝트 구현 |

---

# 3. 시스템 설계 및 아키텍처

## 전체 시스템 구성도

VerseUp은 학생, 강사, 학부모, 관리자의 4-tier 사용자 역할을 지원하는 통합 웹 플랫폼입니다. 최신 웹 기술과 3D 메타버스 기술, 클라우드 인프라를 활용하여 몰입감 있고 확장 가능한 교육 서비스를 제공합니다.

### 멀티테넌트 플랫폼 아키텍처

**학생-강사-학부모-관리자 통합 관리 시스템**

| 학생 앱 | 강사 앱 | 학부모 앱 | 관리자 앱 |
|:------:|:------:|:--------:|:--------:|
| 강의 수강 | 강의 생성 | 자녀 학습 현황 | 사용자 관리 |
| 메타버스 입장 | 출석 관리 | 메타버스 참관 | 강의 관리 |
| 과제 제출 | 과제 관리 | 출석/진도 확인 | 결제 관리 |
| 결제 | 실시간 수업 | | 통계 |

### 시스템 레이어 구성

```
┌─────────────────────────────────────────────────┐
│              Presentation Layer                 │
│              React + Tailwind CSS               │
│           상태관리 / 라우팅 / UI/UX             │
└─────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────┐
│               Supabase Backend                  │
│            완전 관리형 백엔드 서비스            │
└─────────────────────────────────────────────────┘
                        ↓
┌───────────┬───────────┬───────────┬─────────────┐
│PostgreSQL │   Auth    │  Storage  │  Realtime   │
└───────────┴───────────┴───────────┴─────────────┘
                        ↓
┌─────────────────────┬───────────────────────────┐
│      결제 API       │      Express.js Server    │
│   Toss Payments     │       + Socket.IO         │
│ 카드결제/간편결제   │   실시간 통신/파일 업로드 │
└─────────────────────┴───────────────────────────┘
```

## 3-Tier 아키텍처 설계

### 1. Presentation Layer (프레젠테이션 계층)

**React Frontend**

- 학생용 인터페이스 (Student UI)
- 강사용 인터페이스 (Instructor UI)
- 학부모용 인터페이스 (Parent UI)
- 관리자용 인터페이스 (Admin UI)
- 3D 메타버스 환경 (Three.js + React Three Fiber)
- 반응형 웹 디자인

### 2. Application Layer (애플리케이션 계층)

**Supabase + Express.js**

- RESTful API 서버
- Socket.IO 실시간 WebSocket 통신
- 인증 및 권한 관리
- 비즈니스 로직 처리

### 3. Data Layer (데이터 계층)

**PostgreSQL Database**

- 12개 마이그레이션 기반 관계형 데이터베이스
- RLS 보안 정책 적용
- 실시간 데이터 구독
- 파일 스토리지

## 클라이언트-애플리케이션-데이터 티어 상세

### 클라이언트 티어 (Client Tier)

| 사용자 역할별 인터페이스 | React 19 웹 애플리케이션 |
|:----------------------:|:------------------------:|
| 학생 앱 | JavaScript (ES6+) |
| 강사 앱 | Vite 빌드 도구 |
| 학부모 앱 | Tailwind CSS |
| 관리자 앱 | Three.js + R3F (3D) |

### 애플리케이션 티어 (Application Tier)

| 상태 관리 | Express.js 서버 |
|:--------:|:---------------:|
| Zustand 전역상태 | API 라우팅 |
| TanStack Query 서버상태 | 파일 업로드 처리 |
| React Router 라우팅 | CORS 처리 |

### 데이터 티어 (Data Tier)

| Supabase Platform | 외부 서비스 |
|:-----------------:|:----------:|
| PostgreSQL 데이터베이스 | 토스페이먼츠 결제 |
| 실시간 동기화 | |
| 파일 저장소 | |

## 네트워크 구성도

### 프론트엔드 (Frontend Layer)

- React 19 애플리케이션
- 글로벌 CDN 배포
- 자동 SSL/TLS

### 백엔드 (Backend Layer)

- Express.js API 서버
- Socket.IO 실시간 통신
- 비즈니스 로직 처리

### 데이터베이스 (Database Layer)

- PostgreSQL 17
- 실시간 구독
- 자동 백업

### 클라우드 인프라 구성

| 구성 요소 | 설명 |
|:--------:|:-----|
| CDN | Global CDN을 통한 빠른 콘텐츠 배포 |
| SSL/TLS | 자동 인증서 관리 및 HTTPS 강제 |
| 로드밸런싱 | 자동 트래픽 분산 및 고가용성 |
| 모니터링 | 실시간 성능 및 가용성 모니터링 |

### 보안 구성

| 보안 요소 | 설명 |
|:--------:|:-----|
| 방화벽 | 클라우드 레벨 방화벽 설정 |
| 데이터 암호화 | 전송 중 및 저장 시 AES-256 암호화 |
| 접근 제어 | 역할 기반 권한 관리 (RBAC) |
| RLS | Row Level Security를 통한 데이터 접근 제어 |

## 데이터베이스 설계 (ERD)

### 주요 데이터 관계

| 관계 | 설명 |
|:----:|:-----|
| profiles → courses | 강사가 강의를 생성 (instructor_id) |
| profiles → enrollments | 학생이 강의 수강 (student_id) |
| profiles → payments | 사용자별 결제 내역 |
| profiles → attendance | 사용자별 출석 기록 |
| courses → enrollments | 강의별 수강 등록 |
| courses → assignments | 강의별 과제 |
| courses → materials | 강의별 자료 |
| profiles → progress | 학생별 진도 현황 |
| parent_students → profiles | 학부모-학생 연결 |
| invite_codes → profiles | 초대 코드 기반 학부모 등록 |

### 데이터베이스 테이블 상세

#### 사용자 관리 (1 table)

**profiles (사용자 프로필)**

| 컬럼명 | 타입 | 설명 |
|:------:|:----:|:-----|
| id | UUID | PK |
| email | TEXT | UQ, 이메일 |
| role | TEXT | student/instructor/admin/parent |
| name | TEXT | 이름 |
| avatar_url | TEXT | 프로필 이미지 |
| bio | TEXT | 자기소개 |
| is_active | BOOLEAN | 활성화 상태 |
| created_at | TIMESTAMP | 생성일 |
| updated_at | TIMESTAMP | 수정일 |

#### 강의 관리 (2 tables)

**courses (강의 정보)**

| 컬럼명 | 타입 | 설명 |
|:------:|:----:|:-----|
| id | UUID | PK |
| instructor_id | UUID | FK → profiles.id |
| title | TEXT | 강의명 |
| description | TEXT | 설명 |
| thumbnail_url | TEXT | 썸네일 |
| price | NUMERIC | 가격 |
| total_weeks | INTEGER | 총 주차 |
| schedule | TEXT | 일정 |
| is_published | BOOLEAN | 공개 상태 |

**enrollments (수강 등록)**

| 컬럼명 | 타입 | 설명 |
|:------:|:----:|:-----|
| id | UUID | PK |
| course_id | UUID | FK → courses.id |
| student_id | UUID | FK → profiles.id |
| payment_id | UUID | FK → payments.id |
| status | TEXT | 상태 |
| enrolled_at | TIMESTAMP | 등록일 |

#### 강의실 관리 (2 tables)

**classrooms (강의실)**

| 컬럼명 | 타입 | 설명 |
|:------:|:----:|:-----|
| id | UUID | PK |
| course_id | UUID | FK → courses.id |
| room_code | TEXT | UQ, 룸 코드 |
| max_participants | INTEGER | 최대 인원 |
| is_active | BOOLEAN | 활성화 |

**schedules (수업 일정)**

| 컬럼명 | 타입 | 설명 |
|:------:|:----:|:-----|
| id | UUID | PK |
| classroom_id | UUID | FK → classrooms.id |
| title | TEXT | 일정명 |
| day_of_week | INTEGER | 요일 |
| start_time | TIME | 시작 시간 |
| end_time | TIME | 종료 시간 |

#### 결제 관리 (2 tables)

**payments (결제 정보)**

| 컬럼명 | 타입 | 설명 |
|:------:|:----:|:-----|
| id | UUID | PK |
| user_id | UUID | FK → profiles.id |
| course_id | UUID | FK → courses.id |
| payment_key | TEXT | 토스 결제키 |
| order_id | TEXT | 주문번호 |
| amount | NUMERIC | 금액 |
| status | TEXT | 상태 |
| method | TEXT | 결제수단 |
| approved_at | TIMESTAMP | 승인일 |

**refunds (환불 정보)**

| 컬럼명 | 타입 | 설명 |
|:------:|:----:|:-----|
| id | UUID | PK |
| payment_id | UUID | FK → payments.id |
| amount | NUMERIC | 환불 금액 |
| reason | TEXT | 사유 |
| status | TEXT | 상태 |

#### 학습 관리 (3 tables)

**materials (강의 자료)**

| 컬럼명 | 타입 | 설명 |
|:------:|:----:|:-----|
| id | UUID | PK |
| course_id | UUID | FK → courses.id |
| title | TEXT | 자료명 |
| description | TEXT | 설명 |
| file_url | TEXT | 파일 URL |
| file_type | TEXT | 파일 타입 |
| week_number | INTEGER | 주차 |

**assignments (과제)**

| 컬럼명 | 타입 | 설명 |
|:------:|:----:|:-----|
| id | UUID | PK |
| course_id | UUID | FK → courses.id |
| title | TEXT | 과제명 |
| description | TEXT | 설명 |
| due_date | TIMESTAMP | 마감일 |
| max_score | INTEGER | 만점 |

**submissions (과제 제출)**

| 컬럼명 | 타입 | 설명 |
|:------:|:----:|:-----|
| id | UUID | PK |
| assignment_id | UUID | FK → assignments.id |
| student_id | UUID | FK → profiles.id |
| content | TEXT | 제출 내용 |
| file_url | TEXT | 첨부파일 |
| score | INTEGER | 점수 |
| feedback | TEXT | 피드백 |

#### 출석/진도 관리 (2 tables)

**attendance (출석)**

| 컬럼명 | 타입 | 설명 |
|:------:|:----:|:-----|
| id | UUID | PK |
| course_id | UUID | FK → courses.id |
| student_id | UUID | FK → profiles.id |
| session_date | DATE | 수업일 |
| status | TEXT | present/absent/late |
| check_in_time | TIMESTAMP | 체크인 시간 |

**progress (진도)**

| 컬럼명 | 타입 | 설명 |
|:------:|:----:|:-----|
| id | UUID | PK |
| course_id | UUID | FK → courses.id |
| student_id | UUID | FK → profiles.id |
| completed_weeks | INTEGER[] | 완료 주차 |
| current_week | INTEGER | 현재 주차 |
| completion_rate | NUMERIC | 완료율 |

#### 학부모 시스템 (2 tables)

**invite_codes (초대 코드)**

| 컬럼명 | 타입 | 설명 |
|:------:|:----:|:-----|
| id | UUID | PK |
| student_id | UUID | FK → profiles.id |
| code | TEXT | UQ, 초대 코드 |
| is_used | BOOLEAN | 사용 여부 |
| expires_at | TIMESTAMP | 만료일 |

**parent_students (학부모-학생 연결)**

| 컬럼명 | 타입 | 설명 |
|:------:|:----:|:-----|
| id | UUID | PK |
| parent_id | UUID | FK → profiles.id |
| student_id | UUID | FK → profiles.id |
| relationship | TEXT | 관계 |

#### 알림 시스템 (1 table)

**notifications (알림)**

| 컬럼명 | 타입 | 설명 |
|:------:|:----:|:-----|
| id | UUID | PK |
| user_id | UUID | FK → profiles.id |
| type | TEXT | 알림 유형 |
| title | TEXT | 제목 |
| message | TEXT | 내용 |
| is_read | BOOLEAN | 읽음 여부 |
| data | JSONB | 추가 데이터 |

## 기술 스택 및 개발 환경

### 프론트엔드 기술 스택

| 구분 | 기술 |
|:----:|:-----|
| Framework | React 19 |
| Language | JavaScript (ES6+) |
| Bundler | Vite 6 |
| Styling | Tailwind CSS |
| State | Zustand |
| Data Fetching | TanStack Query v5 |
| Routing | React Router v7 |
| 3D Engine | Three.js + React Three Fiber |
| Physics | Rapier |
| Animation | Framer Motion |

### 백엔드 기술 스택

| 구분 | 기술 |
|:----:|:-----|
| Database | Supabase (PostgreSQL) |
| Auth | Supabase Auth |
| Realtime | Socket.IO 4 |
| Storage | Supabase Storage |
| API Server | Express.js 5 |
| Security | RLS + JWT |

### 외부 API 연동

| API | 용도 |
|:---:|:-----|
| 토스페이먼츠 | 안전한 온라인 결제 처리, 카드/간편결제 지원 |
| Google STUN | WebRTC 연결을 위한 NAT 통과 |

---

# 4. 주요 구현 기능

## 로그인/회원가입 페이지

**기능 설명**: Supabase Auth 기반 안전한 사용자 인증

**주요 특징**:
- 이메일 기반 회원가입/로그인
- 4가지 역할 선택 (학생/강사/관리자/학부모)
- 학부모 전용 초대 코드 기반 회원가입
- 자동 세션 관리 및 토큰 갱신
- 비밀번호 재설정 기능

## 학생용 기능 구현

### 대시보드

- 수강 중인 강의 목록 확인
- 다음 수업 일정 표시
- 미제출 과제 알림
- 최근 알림 표시

### 강의 목록/상세

- 전체 강의 검색 및 필터링
- 강의 상세 정보 확인 (커리큘럼, 강사 정보)
- 수강 신청 및 결제

### 메타버스 강의실

- 3D 가상 교실 입장
- 아바타 캐릭터로 이동 (WASD + Shift 달리기)
- 실시간 화면 공유 시청
- 화이트보드 공유 시청
- 실시간 채팅
- 다른 학생들과 상호작용

### 과제 관리

- 과제 목록 확인
- 과제 상세 및 제출
- 파일 첨부 기능
- 점수 및 피드백 확인

### 결제

- 토스페이먼츠 연동 결제
- 카드/간편결제 지원
- 결제 내역 조회
- 결제 상태 확인

## 강사용 기능 구현

### 대시보드

- 담당 강의 목록
- 오늘의 수업 일정
- 수강생 현황
- 미채점 과제 알림

### 강의 관리

- 강의 생성/수정/삭제
- 강의 자료 업로드 (PDF, 이미지, 동영상)
- 커리큘럼 구성
- 강의 공개/비공개 설정

### 출석 관리

- 수업별 출석 체크
- 출석/지각/결석 처리
- 출석률 통계 확인
- 일괄 출석 처리

### 진도 관리

- 학생별 진도 확인
- 주차별 완료 처리
- 진도율 통계
- 일괄 진도 업데이트

### 과제 관리

- 과제 출제 (제목, 설명, 마감일, 배점)
- 제출물 확인
- 점수 부여 및 피드백 작성

### 메타버스 수업

- 3D 가상 교실 생성
- 화면 공유 시작/중지
- 화이트보드 제어 (그리기, 지우기)
- 실시간 채팅
- 참여자 목록 확인

## 관리자용 기능 구현

### 통합 대시보드

- 전체 사용자 통계 (역할별 현황)
- 전체 강의 통계 (공개/비공개)
- 전체 결제 통계 (매출 현황)
- 시스템 상태 모니터링

### 사용자 관리

- 사용자 목록 조회 (페이지네이션)
- 역할 변경 (학생↔강사)
- 계정 활성화/비활성화
- 사용자 검색 (이름, 이메일)

### 강의 관리

- 전체 강의 목록 조회
- 강의 승인/반려
- 강의 상태 변경
- 강의 삭제

### 수강 관리

- 수강 현황 조회
- 수강 취소 처리
- 수강 이력 관리

### 결제 관리

- 결제 내역 조회
- 환불 처리
- 결제 통계

### 강의실 관리

- 강의실 생성/삭제
- 강의실 상태 관리
- 참여자 관리

## 학부모용 기능 구현

### 대시보드

- 연결된 자녀 목록 확인
- 자녀별 수강 현황 요약
- 자녀별 출석률/진도율 그래프
- 최근 알림

### 자녀 연결

- 초대 코드 입력으로 자녀 연결
- 학생이 생성한 초대 코드 사용
- 다수 자녀 연결 가능

### 학습 현황 조회

- 자녀의 수강 강의 목록
- 출석 현황 상세 확인
- 진도 현황 상세 확인
- 과제 제출 현황 확인
- 과제 점수 확인

### 메타버스 참관

- 자녀 수업 참관 모드로 입장
- 투명 아바타로 입장 (다른 사용자에게 보이지 않음)
- 수업 내용 실시간 시청 (화면 공유, 화이트보드)
- 상호작용 제한 (채팅 불가, 이동만 가능)

---

# 5. 핵심 기술 구현

## 실시간 시스템 구현 - Socket.IO 기반 실시간 통신

```javascript
// 실시간 멀티플레이어 위치 동기화
socket.on('player:move', (data) => {
  // 다른 플레이어에게 위치 브로드캐스트
  socket.to(data.roomId).emit('player:moved', {
    odId: socket.id,
    position: data.position,
    rotation: data.rotation,
    animation: data.animation
  });
});

// 실시간 채팅
socket.on('chat:message', (data) => {
  io.to(data.roomId).emit('chat:new-message', {
    userId: data.userId,
    userName: data.userName,
    message: data.message,
    timestamp: new Date()
  });
});

// 화면 공유 시그널링
socket.on('screen:start', (data) => {
  socket.to(data.roomId).emit('screen:started', {
    odId: socket.id,
    userName: data.userName
  });
});
```

**주요 특징**:
- **지연 시간 최소화**: 실시간 데이터 동기화
- **자동 재연결**: 네트워크 끊김 시 자동 복구
- **Room 기반 통신**: 강의실별 격리된 통신 채널

## 3D 메타버스 구현 - React Three Fiber + Rapier

```javascript
// 3D 씬 구성
<Canvas>
  <Physics gravity={[0, -20, 0]}>
    <MapModel />           // 3D 교실 환경
    <Player />              // 플레이어 캐릭터
    <OtherPlayers />       // 다른 참여자들
    <Screen />              // 화면 공유 스크린
    <Blackboard />         // 화이트보드
    <InteractiveObjects /> // 상호작용 오브젝트
  </Physics>
  <ThirdPersonCamera />    // 3인칭 카메라
</Canvas>

// 캐릭터 물리 설정
const PLAYER_CONFIG = {
  capsuleHalfHeight: 0.8,
  capsuleRadius: 0.52,
  walkSpeed: 8,
  runSpeed: 18,
  stepUpSpeed: 4
};

// 캐릭터 이동 처리
const movePlayer = (direction, delta) => {
  const velocity = rigidBody.current.linvel();
  const speed = isRunning ? PLAYER_CONFIG.runSpeed : PLAYER_CONFIG.walkSpeed;

  rigidBody.current.setLinvel({
    x: direction.x * speed,
    y: velocity.y,
    z: direction.z * speed
  });
};
```

**주요 특징**:
- **Rapier 물리 엔진**: 충돌 감지 및 물리 시뮬레이션
- **캐릭터 애니메이션**: Idle/Walk/Run 상태 전환
- **포인터 락 카메라**: 몰입감 있는 3인칭 시점
- **GLTF/GLB 모델 로딩**: 최적화된 3D 모델 사용

## 결제 시스템 연동 - 토스페이먼츠 API 연동

```javascript
// 토스페이먼츠 결제 요청
const requestPayment = async (courseInfo) => {
  const payment = tossPayments.payment({
    customerKey: TossPayments.ANONYMOUS
  });

  await payment.requestPayment({
    method: "CARD",
    amount: { currency: "KRW", value: courseInfo.price },
    orderId: courseInfo.orderId,
    orderName: courseInfo.title,
    successUrl: `${window.location.origin}/payment/success`,
    failUrl: `${window.location.origin}/payment/fail`
  });
};

// 결제 승인 처리 (서버)
const confirmPayment = async (paymentKey, orderId, amount) => {
  const response = await fetch('https://api.tosspayments.com/v1/payments/confirm', {
    method: 'POST',
    headers: {
      'Authorization': `Basic ${Buffer.from(secretKey + ':').toString('base64')}`,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({ paymentKey, orderId, amount })
  });

  return response.json();
};
```

**주요 특징**:
- **PCI DSS 준수**: 카드 정보 보안 표준 준수
- **다양한 결제 수단**: 카드, 간편결제 지원
- **실시간 검증**: 결제 완료 후 즉시 수강 등록

## 인증 및 보안 시스템 - RLS 정책 및 JWT 토큰 관리

```sql
-- 학생은 자신의 수강 정보만 조회
CREATE POLICY "학생은 자신의 수강 정보만 조회"
ON enrollments FOR SELECT
USING (
  auth.uid() = student_id
);

-- 강사는 담당 강의 수강생 조회 가능
CREATE POLICY "강사는 담당 강의 수강생 조회"
ON enrollments FOR SELECT
USING (
  EXISTS (
    SELECT 1 FROM courses
    WHERE courses.id = enrollments.course_id
    AND courses.instructor_id = auth.uid()
  )
);

-- 학부모는 연결된 자녀 정보만 조회
CREATE POLICY "학부모는 자녀 정보 조회"
ON enrollments FOR SELECT
USING (
  EXISTS (
    SELECT 1 FROM parent_students
    WHERE parent_students.parent_id = auth.uid()
    AND parent_students.student_id = enrollments.student_id
  )
);

-- 관리자는 모든 데이터 조회 가능
CREATE POLICY "관리자 전체 접근"
ON enrollments FOR ALL
USING (
  EXISTS (
    SELECT 1 FROM profiles
    WHERE profiles.id = auth.uid()
    AND profiles.role = 'admin'
  )
);
```

**주요 특징**:
- **행 단위 보안**: 사용자별 데이터 접근 제어
- **자동 토큰 갱신**: JWT 토큰 만료 전 자동 갱신
- **권한 기반 라우팅**: 역할별 페이지 접근 제어

## WebRTC 화면 공유 구현

```javascript
// 화면 공유 시작 (강사)
const startScreenShare = async () => {
  const stream = await navigator.mediaDevices.getDisplayMedia({
    video: {
      cursor: 'always',
      displaySurface: 'monitor'
    },
    audio: false
  });

  // 스트림 종료 감지
  stream.getVideoTracks()[0].onended = () => {
    stopScreenShare();
  };

  // Socket.IO를 통해 시그널링
  socketService.emit('screen:start', { roomId });

  return stream;
};

// WebRTC Peer Connection 설정
const createPeerConnection = () => {
  const peerConnection = new RTCPeerConnection({
    iceServers: [
      { urls: 'stun:stun.l.google.com:19302' },
      { urls: 'stun:stun1.l.google.com:19302' }
    ]
  });

  peerConnection.onicecandidate = (event) => {
    if (event.candidate) {
      socketService.emit('webrtc:ice-candidate', {
        candidate: event.candidate,
        targetId: targetSocketId
      });
    }
  };

  return peerConnection;
};
```

---

# 6. 배포

## 배포 환경 및 CI/CD 구성

```yaml
# 배포 구성
services:
  - type: web
    name: verseup
    env: node
    buildCommand: npm install && npm run build
    startCommand: node server/server.js
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_SERVICE_KEY
        sync: false
```

**CI/CD 특징**:
- **자동 빌드**: GitHub 코드 푸시 시 자동 빌드 실행
- **테스트 자동화**: 빌드 전 린트 검사 실행
- **무중단 배포**: 헬스체크 기반 안전한 배포
- **롤백 지원**: 배포 실패 시 이전 버전으로 자동 롤백

## 모니터링 및 운영 관리

| 모니터링 항목 | 설명 |
|:-----------:|:-----|
| 애플리케이션 성능 | 응답 시간, 처리량, 오류율 실시간 모니터링 |
| 인프라 상태 | CPU, 메모리, 네트워크 사용률 추적 |
| 사용자 활동 | 실시간 접속자, 강의실 참여 현황 |
| 로그 관리 | 중앙집중식 로그 수집 및 분석 |
| 알림 시스템 | 임계치 초과 시 즉시 알림 발송 |
| 백업 전략 | 일일 자동 백업 |

---

# 7. 개별 후기

## 안지민 후기

(직접 작성)

## 이재원 후기

(직접 작성)

## 조민혁 후기

(직접 작성)

---

## 부록: 스크린샷 목록

| 번호 | 화면명 | 설명 |
|:----:|:------|:-----|
| 1 | 로그인/회원가입 | 사용자 인증 화면 |
| 2 | 학생 대시보드 | 학생용 메인 화면 |
| 3 | 강의 목록 | 전체 강의 목록 페이지 |
| 4 | 강의 상세 | 강의 정보 및 수강 신청 |
| 5 | 결제 화면 | 토스페이먼츠 결제 |
| 6 | 메타버스 강의실 | 3D 가상 교실 환경 |
| 7 | 화면 공유 수업 | 강사 화면 공유 중인 모습 |
| 8 | 강사 대시보드 | 강사용 메인 화면 |
| 9 | 출석 관리 | 출석 체크 화면 |
| 10 | 진도 관리 | 학생별 진도 현황 |
| 11 | 과제 관리 | 과제 목록 및 채점 |
| 12 | 관리자 대시보드 | 관리자용 통계 화면 |
| 13 | 사용자 관리 | 사용자 목록 및 관리 |
| 14 | 학부모 대시보드 | 학부모용 메인 화면 |
| 15 | 학부모 참관 모드 | 수업 참관 중인 모습 |
